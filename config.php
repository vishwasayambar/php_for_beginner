<?php
/* We can use return direct in plane files also not only in function
Also this config files is not specific to database it can be for any service we need dynamic so we have to a key to each config like database for Database config


*/
return [

    'database' => [
        'host' => '127.0.0.1',
        'port' => '3306',
        'dbname' => 'learning',
        'charset' => 'utf8mb4'
    ],

];
